<h1><%= @user.email %></h1>

<div class="row">
  <div class="col-md-8">
    <% if current_user == @user %>
      <%= render partial: 'items/form' %>
    <% end %>
  </div>
</div>
<% @items.each do |item| %>
  <%= render partial: 'items/item', locals: { item: item } %>
<% end %>